name: mesa
on:
  push:
    branches: [main, sdm845-odin]
    paths: ['mesa/**', 'oci/**', 'scripts/**', 'toolchain.env', '.forgejo/workflows/mesa.yml', '.forgejo/workflows/_package-image.yml']
  workflow_dispatch:
jobs:
  build:
    uses: ./.forgejo/workflows/_package-image.yml
    secrets: inherit
    with:
      package: mesa
