name: inputplumber
on:
  push:
    branches: [main, sdm845-odin]
    paths: ['inputplumber/**', 'oci/**', 'scripts/**', 'toolchain.env', '.forgejo/workflows/inputplumber.yml', '.forgejo/workflows/_package-image.yml']
  workflow_dispatch:
jobs:
  build:
    uses: ./.forgejo/workflows/_package-image.yml
    secrets: inherit
    with:
      package: inputplumber
